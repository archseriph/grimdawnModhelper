# third_party submodules

This repository tracks several third-party tools useful for Grim Dawn modding. The submodules are configured to track the upstream branches declared in .gitmodules. To initialize them locally, run the script scripts/add_submodules.sh (or follow the commands below).
